db:
    password: big secret
some.prop: why do I have to set this on my local
mail:
    sender: dummy

##########################################################################################
spring:
    mail:
        host: smtp.gmail.com
        password: <login password to smtp server>
        port: 587
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
        username: <login user to smtp server>
    datasource:
        hikari:
            auto-commit: true
    config:
        import: classpath:/this-too.yaml


clear.cron: 0 0 0 * * *

